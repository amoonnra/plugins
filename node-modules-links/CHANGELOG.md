# Changelog

## 0.1.0

- Add native links for dependency names in `package.json` and JSONC manifests.
- Navigate to installed package manifests and reveal package folders in the Explorer.
- Resolve scoped packages, aliases, symlinks, and hoisted dependencies using ancestor `node_modules` directories.
- Include the Marketplace icon, publishing metadata, and local VSIX build configuration.
