{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Node Modules Links",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}"],
      "outFiles": ["${workspaceFolder}/dist/**/*.js"],
      "preLaunchTask": "npm: bundle"
    }
  ]
}
